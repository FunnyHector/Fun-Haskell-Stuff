MakeGraph: I could go through two lists (the list of vertices, and the list
of edges) only once, but that approach would significantly damage the readablity
of function. The submitted approach is pretty straight forword to read, but for
each error type 
